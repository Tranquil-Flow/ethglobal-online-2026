# Local composition deliberately ignores .env files and discovers no credentials.
# Use explicit CLI flags, not live-mode variables:
# npm start -- --development --data-dir /absolute/private/ethonline-development --port 4310
# Never put wallet keys, session capabilities, RPC tokens or Graph credentials here.
# Future live adapters require separately approved configuration and qualification.
