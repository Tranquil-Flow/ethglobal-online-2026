# Verification-independent application completion

## Integration successor update (2026-09-11)

See `MANAGED_INTEGRATIONS.md` for the implemented managed publisher/budget/journal closure, hosted History RPC, private CLI/MCP pins and non-economic submission policy, provider payment factory, and method-neutral assessor/artifact lifecycle. These are exercised local integration paths, not a selected verifier or protected-finance release. Historical statements below about wholly missing adapters or wholly absent public deployments must not override this later source and its final receipt. The earlier native success and public Registry/Graph assets remain real; a new managed native start failed because A's advertised socket/processes were absent. Current real native/public qualification remains blocked on A's maintained binding/topology and any exact remaining activation authority. Do not infer completion until the adjacent `NONVERIFICATION-INTEGRATION-HANDOFF.json` binds final source and observed gates.


Authority: continuation `hackathon-app-02/GOAL.md`, from preserved buyer anchor `2d5402e3963e43d62adb3e4248f9ded5e1a90c9b`. Application integration ownership is limited to this successor. Predecessor packets and research remain separate. Historical receipts are not final-candidate evidence.

## Current disposition

**Successor 03: non-verification completion is now owner-authorized and in progress.** The direct instruction in session `20260910_124347_7069ff`, message `627815`, supersedes preparation-only authority for this application scope. Native/heavy execution remains coordinated through existing A; inference verification and dependent financial guarantees remain separate and disabled. No unchecked real-model, independent-provider or public-service completion is claimed until exercised. Historical milestones below remain historical.


**Application-only implementation verified at `8a6fee1`; final qualification is resolved by the exact-candidate `../APPLICATION-HANDOFF.json` packet. An absent or unsuccessful final packet is not local completion.** Do not infer release, selected-model execution or financial protection from these results.

## Canonical R1–R12 ledger

Paths below are relative to this checkout. `composition/test/` is discovered by the existing canonical composition runner; no parallel always-green acceptance runner exists.

| ID | Implemented source / user path | Local acceptance | Separate unqualified gate |
|---|---|---|---|
| R1 | `composition/application-workbench.mjs`, core HTTP/OpenAI, access SDK, encrypted recovery | `application-serving`, `application-recovery`, `application-journey`, existing `openai-heartbeat` composition tests | Actual accepted model/profile and external endpoint |
| R2 | Managed independent identities and native primary-only configuration; quickstart links pinned native invite/join/revoke primitives | `application-operator`, `operator-unchecked`, provider-isolation tests; two synthetic provider identities | Native-owned physical membership, admission and coalition operation |
| R3 | Authenticated bounded offers, provider/profile choice, scoped runtime/signing/stores; offered-profile CLI/MCP; managed optional ENSv2 binding | `application-serving`, `application-client-parity`, `application-history`, `application-ens`; existing real local ENS/Graph composition | Current external ENS/resource pins and genuinely independent physical operators |
| R4 | Provider-scoped transport/observations and original request/output attribution; no heartbeat-as-inference claim | Serving/native gateway and retained receipt regressions | Actual assigned/participating physical execution |
| R5 | Immutable buyer context/evidence, explicit assessment lifecycle, redacted failures and offline integrity | `application-assessment`, browser/recovery and buyer-bound evidence tests | Selected research method/version/coverage and measured computation claim |
| R6 | Real local dual-registry Graph ingestion and history into application choice | `application-journey`, `application-history`, canonical indexing `smoke:ingestion`, reorg/reindex gate | Authorized hosted Graph deployment and current indexed provenance |
| R7 | Old bond remains retired; no resurrection or financial authority from history | Preserved schema/financial-refusal regressions | None |
| R8 | `application-operator.mjs` init/doctor/start, OS lock, bounded lifecycle, preflight, restart, safe errors | Operator/preflight, recovery, existing cancellation/deadline/cleanup tests | Actual host/model envelope and fresh native grant |
| R9 | Finite `demo:application`, runnable managed start, explicit HTTPS public-origin configuration and existing bounded TLS proxy | `application-demo.mjs`, `application-journey`, `application-https` with actual local verified TLS | Approved multi-host/public HTTPS/testnet demonstration |
| R10 | Pinned setup, encrypted full-state closure, declared historical signing keys, fresh-target staged restore | Clean export setup/demo passed at 8a6fee1; `application-backup`, `application-rotation-backup`, operations v1/v2 tests | Exact final runtime/model/application licenses and physical reproduction |
| R11 | `RegistryV2.sol`, EIP-712 checker statements, ABI/mapping/history; legacy v1 preserved | Actual independent checker/relayer publication without prior receipt in `application-journey`; indexed unlinked claim affects explanation, not blacklist; signatures/replay/reorg tests | Versioned public registry/hosted ingestion deployment |
| R12 | No-checker serving and safe unavailable/malformed/timeout states; qualified financial modes still refused | `application-assessment`, `operator-unchecked`, existing protected-payment refusal tests | C-owned canonical result authentication, objective adjudication, settlement/economics, public financial qualification |

The application offers stream-first provisional output, not check-before-use or protected paid service. Unlinked authenticated claims remain `unlinkedClaims`, never authoritative mismatches. Native N and prospective O remain distinct; B screening is optional, not a release or financial prerequisite.

## Exercised integrated journey

`composition/test/application-journey.test.mjs` creates a fresh private managed state with two identities and actual local Graph/Anvil infrastructure. It runs a real browser, exports encrypted recovery before submission, submits and retains synthetic output, exports original context/evidence and verifies integrity, restarts/reloads and reconciles the same job, invokes the unmodified installed stock OpenAI SDK for both configured providers, submits an explicitly consented synthetic adverse checker statement from another key with a separate relayer, waits for actual indexed history, and checks the next real client decision. It then performs encrypted backup/restore through the operator CLI, starts the restored application, recovers the same evidence and deletes server-side private evidence. Its `finally` closes its browser/application/Graph/EVM and removes only temporary state.

Additional gates cover uncertain accepted response loss, interrupted SSE, double submission, cross-buyer/provider rejection, bounded expiry/revocation, corrupt/wrong-provider snapshots, wrong assessment association, delayed/repeated/serialized observations, port/private-file rejection and TLS host/origin boundaries. Test checker outcomes and synthetic executors establish application lifecycle only.

## Evidence and failure adjudication

Continuation commands/logs are append-only in `../commands.jsonl` and `../logs/`. `docs/handoffs/application-verification.json` records the executed clean source checks at 8a6fee1. The adjacent successor `APPLICATION-HANDOFF.json` records later exact-final-commit canonical results without a self-referential Git receipt.

- `full-application-journey-03`: integrated local journey passed on the then-dirty candidate. Earlier attempts failed due test-oracle/API invocation mistakes; retained, not presented as product successes.
- `open-indexed-journey-03`: actual v2 ingestion, attribution, client-decision and reorg path passed. Earlier dual-registry launcher failures remain retained.
- `offered-client-parity-red` → `offered-client-parity-green`: missing CLI offered-profile selection repaired; actual CLI/MCP application path passed.
- `recovery-race-red` → `recovery-race-green`: changed input during pending recovery registration previously downloaded a stale attempt. Now stale preparation is discarded, revocation status is explicit, and no stale file/new job is produced.
- `historical-key-closure-red` → `historical-key-closure-green`: explicit historical private-key declarations are bound to persisted provider/public-key history and survive encrypted restore. Unknown/undeclared files remain rejected.
- `lifecycle-expanded-03`: malformed profile/checker/evidence association, timeout and overlapping same-job requests passed. Earlier out-of-order test incorrectly assumed parallel assessor calls: the actual API deliberately serializes per-job requests. No production guard was weakened to satisfy that oracle.
- `tls-delivery-entrypoint-02`: actual loopback HTTPS entrypoint, independently trusted ephemeral test CA, signed public-origin offers and recovery passed. Certificate/key permission failures are retained; no TLS verification bypass.

Two bounded independent reviews (`deleg_52e4f390`, `deleg_4b1a03c3`) timed out without final verdicts; their retained transcript endings were inspected, and neither remains an outstanding successful review. Its surviving browser-race and rotated-key reproductions were inspected; the integrating owner reproduced/adjudicated both and added canonical regressions. Partial review is not a blanket independent approval.

## Delivery and one remaining activation request

Use `APPLICATION_QUICKSTART.md`, `APPLICATION_PORTABILITY.md`, `VERIFICATION_INTEGRATION.md` and `SUBMISSION-DRAFT.md`. Canonical required commands are `npm run check:all`, `npm run smoke:integration`, `npm run check:operations`, and `npm --prefix packages/indexing run smoke:ingestion`, followed by exact-source/cleanup reconciliation.

**After local closure, the bounded owner request is:** A/native owner supplies the accepted exact producer/profile/qualification/transport pins and a fresh finite resource grant; C supplies any selected verifier/financial contract and its authentication/coverage/economic acceptance criteria; D/product explicitly adopts the compatible relation. The deployment owner separately approves exact hosts, origins/certificates/ENS/Graph/registry targets, public testnet budgets and licensing/visibility/submission scope. Only then execute a separately scoped real-model/verification/external qualification through these application entrypoints. No old grant, source-only conformance, signed claim or test balance renews that authority. Financial modes stay refused until their distinct accepted interface and end-to-end gate pass.

### Final-gate repair adjudication

`candidate-check-all-01` failed overall at committed 66569c1, despite 134/134 composition tests passing. Four package handoffs referred to superseded implementation revisions. The access package also had stale MCP tool-inventory expectations and two legacy-v1 fixture tests incorrectly expecting v2 recovery registration. `access-oracle-repair-01` passed all four focused tests after restoring their v1 scope and adding the intended tool to the exact inventory. No production guard or v2 recovery regression was removed.

The R3 reconciliation then identified and closed a genuine managed-v2 configuration gap: optional ENS was only available in the older composition. `managed-ens-red` reproduced the missing field; `managed-ens-green-03` passed six relevant tests including actual ENS record changes, failed selection without redirection, direct signed offers and offline doctor. Two intermediate test-oracle mistakes (expected selection-null versus explicit `PROVIDER_CHANGED`, and doctor `ok` versus literal `status`) were retained and corrected without weakening application guards.

### Frozen source evidence

`closure02-*` at clean committed 8a6fee1 passed all five package checks/smokes, the complete 135-test composition suite, operations (24 plus 1 tests), and actual local v1/v2 Graph ingestion. `clean-setup-demo-02` passed setup and the finite two-provider demo from a fresh source export. There were no skipped/cancelled/failed tests in those TAP summaries. Non-TAP smoke commands have no invented test counts. The later documentation/handoff-only commit must pass all four required canonical commands before the final completion status can be declared.

## Successor 03 — remaining end-to-end qualification

`docs/qualification/CASE-REGISTRY.json` is the single logical case inventory for the enlarged goal. Its provider roles are unresolved placeholders, all real-world groups remain NOT_RUN, and no null command/resource ceiling is executable. A must bind and price concrete groups before a new grant. The original R1–R12 table above remains the implementation crosswalk; this section does not relabel its local evidence as physical/public qualification.

- R1/R3/R8: native readiness previously occurred before occupied-port/private-state preflight, and provider A could be contacted before provider B's grant failed. `native-start-admission-red` reproduced two failures; lazy async runtime construction plus all-grants-first admission repaired them. Added private-lock and positive model-shaped HTTP controls; no model executed.
- R1/R3/R10: `plan-native`/`init-native` now construct private pinned native configuration without manual profile-digest copying, an operative grant or a credential. Whole-catalog/permission/expiry/target-safety controls and missing-grant startup are exercised through the normal operator CLI. `native-import-red` retained the missing CLI path.
- R1/R5: additive native metadata v2 can state raw-logit greedy/lowest-ID/nonfinite rejection without falsely declaring quantization. V1 fixture Profile ID is unchanged; the legacy candidate protocol refuses raw v2 metadata. This represents policy only, not empirical conformance of any producer.
- R3/R8/R9/R10: offline app/TLS deployment planning, public-pin export, certificate/key/name/time checks and reused real-TLS acceptance are exercised. No listener or runtime contact during planning; certificate-chain/public endpoint qualification stays false.
- R6/R11: explicit RegistryV2 offline planning and deployed receipt/code inspection use the existing compiler/EVM stack. Real local planned calldata, complete immutable substitution, finalized receipt/runtime checks, reorg/unknown/pending cases, altered frozen plans, safe RPC deadlines and bounded file reads are tested. Legacy Registry and RegistryV2 template hashes are unchanged. The timed-out worker return was treated as partial source, reviewed and rerun by the integrator; additional review failures remain retained.
- R2/R4/R7/R12: native membership authority and physical attribution remain owner-scoped; old bond remains retired. No accepted financial interface was invented or activated, and no signature/history event can unlock funds.

Current public metadata read: A06 remains source-only/full-programme-blocked; C full-verify-05 provides scoped fixtures/historical/prepared/blocked results; D full-programme-review-02 records no operative grant. The historical A stdio application adapter exists and used development mode; it is not a ready v3 gateway packet. The owned `producer-profile-discriminator` accepted A's public artifact as the shared Profile DTO and rejected treating it as an operator/grant packet. That is a format check, not an assertion that no producer exists. A must admit the specific serving binding/mode and exact codec/termination semantics. C/A/D must supply the accepted checking/financial contracts; deployment/native owners must supply operator identities, targets and authority.

### Latest owner/resource reconciliation and honest local gate state

After source freeze at `0d0c7cd`, newer A07/C06 and D execution-focus/coordinator metadata were discovered. C06 supplies the corrected five algorithm entrypoints; old missing-entrypoint findings are historical, not current accusations. C still explicitly lacks the relevant layer/prefix proof programmes/VKeys/adjudicators and authenticated O Cartesi guest/voucher/dispute composition. A's historical stdio path and its separate N proposal are not a new serving grant.

**C-NATIVE-FEASIBILITY-1 is already owner-approved to A and delivery/admission activity is recorded by the coordinator. Do not ask for or resend that approval.** It neither authorizes this application/N-serving nor supplies protected settlement or public deployment approval.

All five package check/smoke pairs and the short operations gate passed on the clean frozen implementation. `source03-integration` was intentionally interrupted with exit 1 to yield shared resources to A: its emitted pass prefix is not a complete suite result. The exact test process group and Graph/Postgres/IPFS objects were identified; Graph's RPC port matched the owned Anvil process, and cleanup was independently checked. No A process or research data was touched. Full combined integration, aggregate `check:all`, ingestion and fresh final-source reproduction remain pending an A-coordinated quiet window. Lane handoffs are explicitly blocked rather than laundering prior composition results.

The tracked summary is `docs/handoffs/end-to-end-preparation.json`. Detailed interruption/cleanup and updated metadata hashes are in the adjacent `RESOURCE-COORDINATION.json`. The single staged owner request is `OWNER-INPUT-AND-AUTHORITY-REQUEST.md`; it is prepared, not sent, and creates no operative permit. Runtime/reference/financial acceptance, independently controlled operators, and actual public targets/authority remain unresolved separately from the local test window.
