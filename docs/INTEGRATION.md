# Integration owner — start after workers stop

Work only in /Users/evinova-self/Projects/ethglobal-online-2026 main, not another lane's checkout.
First inspect worktree list, branches, dirt and docs/handoffs on each actual lane branch. Do not
trust the stale not_started copies on main before merging. Read exact diff/commit and evidence.
Check that all workers have stopped writing; otherwise do read-only review, not merge.

1. Run each lane's actual test/check/smoke at its candidate; inspect shared-contract requests.
2. Adjudicate shared changes with versioned fixtures and compatibility tests. Do not erase errors
   or weaken validation merely to combine code. Unknown external qualification stays explicit.
3. Merge local lane commits in order payments, discovery, indexing, core, access. Preserve their
   history and human authorship. Resolve only understood conflicts; never reset dirty worktrees.
4. Own composition/ (new), root scripts/CI/docs and shared contracts. Wire all real package ports
   into core, with explicitly synthetic development execution and unavailable assessment adapter.
5. Install package lockfiles cleanly. Exercise actual SDK -> HTTP -> quote/payment -> durable job ->
   receipt -> history/viewer paths. Deterministic development payment tests remain visibly development;
   no local fake is a live paid service. Exercise failure/restart/idempotency/privacy/outbox boundaries.
6. Verify browser/CLI/MCP refer to the same job; meaningful provider selection uses real-shaped
   history and reports unknown/stale correctly. Run npm run check:all and new composition smoke.
7. Add CI for full application, clean-checkout setup, safe .env.example, operator scripts, exact
   external gate runbook and accurate README/demo/AI/reuse docs. Avoid claiming untested live work.
8. Ask user for a single consolidated external gate list: license/publication, eligibility, funded
   testnet budgets, ENS authorization, Graph account/deployment. No mainnet, public writes or pushes.

Done: fully exercised local combined app excluding Mycelium/Gas Killer, accurate remaining gates,
reviewed committed candidate, and explicit adapter onboarding instructions for future sessions.
Do not implement those runtime integrations as a way to satisfy this goal.
