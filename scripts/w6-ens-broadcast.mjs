// Retired: this wrapper used to hardcode approved:true and stale target values.
// Preview with scripts/w6-ens-preview.mjs. After explicit owner approval, use
// composition/ens-wave6-repoint.mjs with --target, --wallet, --journal,
// --execute, --approved and --approved-target <exact reviewed targetDigest>.
console.error('RETIRED_UNSAFE_DRIVER_USE_DIGEST_BOUND_ENS_CLI');
process.exitCode = 1;
