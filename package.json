{
  "name": "ethglobal-online-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm --prefix packages/contracts test",
    "check": "npm --prefix packages/contracts run check && node scripts/check-layout.mjs && node --test scripts/test-gates.test.mjs",
    "check:lane": "node scripts/check-lane.mjs",
    "check:all": "node scripts/check-all.mjs"
  }
}
